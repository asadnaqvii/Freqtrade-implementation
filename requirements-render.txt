# Runtime dependencies for all three Render services.
#
# TA-Lib ships prebuilt wheels now, so no C toolchain is needed at build time.

freqtrade==2026.7
TA-Lib>=0.6
scipy
ccxt>=4.4

# pandas 3 changed enough that it is worth pinning to the major version
# freqtrade is actually exercised against.
pandas>=2.2,<3
numpy>=2.0,<3

# The application service
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
PyJWT>=2.8

# Postgres driver. Without this freqtrade cannot use a postgresql:// db-url and
# silently falls back to SQLite, which is the failure this whole change exists
# to remove.
psycopg2-binary>=2.9
